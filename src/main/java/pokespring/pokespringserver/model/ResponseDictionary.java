package pokespring.pokespringserver.model;

public abstract class ResponseDictionary extends Dictionary {
    @Override
    public Response findById(String id) {
        return super.findById(id);
    }
}
